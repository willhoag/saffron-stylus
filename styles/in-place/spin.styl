@require '../variables'

@keyframes spin {
  0% {
    transform: rotate(0deg) translateZ(0)
  }
  100% {
    transform: rotate(360deg)
  }
}

spin($duration = 0.5s, $delay = 0s, $fill-mode = none, $count = 1, $timing = ease-in-out)
  animation: spin $count $duration $delay $fill-mode $timing
