@require '../variables'

@keyframes ping {
  0% {
    transform: scale(0)
  }
  100% {
    transform: scale(1)
    opacity: 0;
  }
}

ping($duration = 1.5s, $delay = 0s, $fill-mode = none, $count = infinite, $timing = ease-in-out)
  animation: ping $count $duration $delay $fill-mode $timing
