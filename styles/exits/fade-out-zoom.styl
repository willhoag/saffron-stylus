@require '../variables'

@keyframes fadeOutZoom {
  0% {
    opacity: 1;
    transform: translateZ(0)
  }
  100% {
    opacity: 0;
    transform: scale(0)
  }
}

fadeOutZoom($duration = 1s, $delay = 0s, $fill-mode = forwards)
  animation: fadeOutZoom $duration $delay $fill-mode
