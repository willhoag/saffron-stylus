@require '../variables'

@keyframes riseIn {
  0% {
    opacity: 0;
    transform: translateY($saf-drop-rise-distance) translateZ(0)
  }
  50% {
    opacity: 1
  }
}

riseIn($duration = 0.5s, $delay = 0s, $fill-mode = forwards)
  animation: riseIn $duration $delay $fill-mode